# 04 — 错误：非法导入

预期：**不**渲染组件，显示插件错误面板（`lodash-es` 无法按 `./`、`@/`、`@custom-script/` 规则解析）。

```vue-interactive
<template>
  <p>若看到这段文字，说明错误处理失败。</p>
</template>

<script setup lang="ts">
import { ref } from 'lodash-es'

const x = ref(0)
</script>
```
