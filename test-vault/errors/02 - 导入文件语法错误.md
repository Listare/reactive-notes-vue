# 错误 — 导入文件语法错误

预期：**不**渲染组件；显示编译错误面板，信息应关联被导入的 `fixtures/broken-syntax.ts`；Obsidian 不崩溃。

```vue-interactive
<template>
  <p>若看到这段文字，说明导入文件的语法错误未被捕获。</p>
</template>

<script setup lang="ts">
import { brokenValue } from './fixtures/broken-syntax.ts'
void brokenValue
</script>
```
