---
description: 修改 TS/TSX 后必须用 linter 检查并修复引入的问题
globs: **/*.{ts,tsx}
alwaysApply: false
---

# 改完代码后检查 Linter

编辑或新建源码后，在认为任务完成前，对**刚改过的文件**跑 linter，并修复本次引入的问题。

## 要求

- 使用 IDE 诊断（`ReadLints`）检查刚编辑的文件，不要只依赖「感觉没问题」。
- 范围限定在本次改动相关文件，避免对整个仓库做无意义的全量扫描。
- 若诊断报错/警告明显由本次改动引起，先修好再结束；与本次无关的既有问题可先不动。
- 修完后如仍有疑虑，可再查一次同一批文件，确认已消除。

## 流程

```
1. 完成代码修改
2. 对改动文件读取 linter / 诊断
3. 修复本次引入的问题
4. 必要时复检同一批文件
```

## 示例

```
// ❌ BAD
改完 compiler/assemble.ts 直接说「完成」，未看诊断

// ✅ GOOD
改完后对 assemble.ts（及相关测试）跑 ReadLints，
发现 unused import / 类型错误 → 修好 → 再确认无新问题
```
